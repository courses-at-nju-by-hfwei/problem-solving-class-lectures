% file: algs/bellman-ford-dp.tex

\begin{algorithm}[H]
  % \caption{Bellam-Ford Algorithm as a DP Algorithm.}
  % \label{alg:bellman-ford-dp}
  \begin{algorithmic}[1]
    \Procedure{Bellman-Ford-DP}{$G, w, s$}
      \State $d[0,s] \gets 0$
      \For{$(v \neq s) \in V$}
	\State $d[0,v] \gets \infty$
      \EndFor

      \hStatex
      \For{$i \gets 1 \;\text{\bf to}\; |V| - 1$}
	\For{$v \in V$}
	  \State $d[i,v] = d[i-1, v]$
	  \For{$(u,v) \in E$}
	    \If{$d[i-1, v] > d[i-1, u] + w(u, v)$}
	      \State $d[i, v] = d[i-1, u] + w(u, v)$
	    \EndIf
	  \EndFor
	\EndFor
      \EndFor
    \EndProcedure
  \end{algorithmic}
\end{algorithm}

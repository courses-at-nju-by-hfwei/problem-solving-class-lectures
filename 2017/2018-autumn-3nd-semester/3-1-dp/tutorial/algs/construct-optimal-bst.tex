% file: algs/construct-optimal-bst.tex

\begin{algorithm}[H]
  % \caption{Construct Optimal BST.}
  % \label{alg:construct-optimal-bst}
  \begin{algorithmic}[1]
    \Procedure{Construct-Optimal-BST}{$root$, $i$, $j$}
      \If{\red{$j = i-1$}}
	\State \Return node with \blue{$d_{i-1}$}
      \EndIf

      \hStatex
      \State $\purple{r} \gets \text{ node with key } k_{root[i,j]}$
      \State $r.\textsl{left} \gets \Call{Construct-Optimal-BST}{root, i, root[i,j]-1}$
      \State $r.\textsl{right} \gets \Call{Construct-Optimal-BST}{root, root[i,j]+1, j}$

      \hStatex
      \State \Return $r$
    \EndProcedure
  \end{algorithmic}
\end{algorithm}

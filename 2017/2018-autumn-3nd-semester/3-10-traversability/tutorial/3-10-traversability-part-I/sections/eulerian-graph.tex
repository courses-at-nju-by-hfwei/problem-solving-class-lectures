% file: sections/eulerian-graph.tex

%%%%%%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{theorem}[Leonhard Euler $1735$]
    A connected graph $G$ is \red{Eulerian} if and only if the degree of each vertex of $G$ is even.
  \end{theorem}

  \pause
  \begin{proof}[``$\Longleftarrow$'' (Carl Hierholzer $1873$); \pause\teal{\small (Proofs on PPT from Tao \& on CZ)}]
    \pause
    \begin{center}
      By strong mathematical \red{induction on the number $m$ of edges} of $G$.
    \end{center}
    \pause
    \[
      \text{Inductive Step}: m = k + 1
    \]

    \pause
    \begin{center}
      Let $C = u \sim v$ be any trail in $G$ of the maximum length.
    \end{center}
    \pause
    \[
      v = u \pause \implies C = u \sim u
    \]
    \pause
    \[
      \purple{H = G - E(C) = \bigcup H_i}
    \]
  \end{proof}
\end{frame}
%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%
\begin{frame}{}
  \[
    \purple{H = G - E(C) = \bigcup H_i}
  \]

  \begin{columns}
    \column{0.40\textwidth}
      \fig{width = 0.85\textwidth}{figs/hierholzer-proof}
    \column{0.65\textwidth}
      \pause
      \begin{enumerate}[(I)]
	\item $\forall v \in H: \text{deg}(v)$ is even
	\item $\forall i: \Big\lvert E(H_i) \Big\rvert < m$
      \end{enumerate}

      \pause
      \vspace{0.30cm}
      \begin{center}
	\teal{By I.H., each $H_i$ has an Eulerian circuit $C_i$.}
      \end{center}

      \pause
      \[
	\forall i: V(H_i) \cap V(C) \neq \emptyset
      \]

      \pause
      \begin{center}
	Glue together each $C_i$ with $C$ to get an Eulerian circuit of $G$.
      \end{center}
  \end{columns}
\end{frame}
%%%%%%%%%%%%%%%%%%%%

% file: parts/linear-time-sorting.tex

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{exampleblock}{Sorting $[0, n^3-1]$ (Problem 8.3-4)}
    Sort $n$ integers in $[0, n^3-1]$ in $O(n)$ time.
  \end{exampleblock}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{exampleblock}{Sorting in Place in Linear Time (Problem $8-2\; (e)$)}
    Suppose that the $n$ records have keys in the range $[\teal{0}, k]$.

    Modify \textsc{Counting-Sort} to sort them \red{in place ($O(k)$)} in \red{$O(n + k)$} time.
  \end{exampleblock}

  \begin{columns}
    \column{0.50\textwidth}
      \pause
      \fignocaption{width = 0.70\textwidth}{figs/counting-sort}
    \column{0.50\textwidth}
      \pause
      \fignocaption{width = 0.60\textwidth}{figs/counting-sort-cycle-example}

      \pause
      \uncover<5->{\centerline{\texttt{\teal{While ($i \ge 1$):}}}}
      \fignocaption{width = 0.60\textwidth}{figs/counting-sort-cycle}
  \end{columns}
\end{frame}
%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%
\begin{frame}{}
  \begin{columns}
    \column{0.50\textwidth}
      \fignocaption{width = 0.60\textwidth}{figs/counting-sort-cycle-example}

      \centerline{\texttt{\teal{While ($i \ge 1$):}}}
      \fignocaption{width = 0.60\textwidth}{figs/counting-sort-cycle}
    \column{0.50\textwidth}
      Code here
  \end{columns}
\end{frame}
%%%%%%%%%%%%%%%
